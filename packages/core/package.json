{
  "name": "@conduit8/core",
  "type": "module",
  "version": "1.0.0",
  "license": "AGPL-3.0",
  "description": "Core package with shared domain models, contracts, API schemas, and HTTP client utilities",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "tsdown --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --quiet",
    "lint:fix": "eslint . --fix --quiet",
    "build": "tsdown",
    "test": "vitest run --reporter=dot --silent",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@slack/types": "^2.16.0",
    "camelcase-keys": "^9.1.3",
    "snakecase-keys": "^9.0.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250831.0",
    "@conduit8/eslint-config": "workspace:*",
    "@types/node": "catalog:",
    "@vitest/coverage-istanbul": "catalog:",
    "eslint": "catalog:",
    "tsdown": "^0.15.3",
    "typescript": "catalog:",
    "vite-tsconfig-paths": "catalog:",
    "vitest": "catalog:"
  }
}
